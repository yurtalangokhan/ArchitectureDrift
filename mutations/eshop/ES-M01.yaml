schema_version: "1.0"

mutation_id: ES-M01
system_id: eshop

description: >
  Introduce an unexpected CALLS relation from Basket API to Catalog API.

rationale: >
  Runtime-visible controlled service dependency.

operations:
  - operation: ADD_RELATION
    relation:
      source: basket-api
      relation: CALLS
      target: catalog-api

expected_delta:
  added_relations:
    - source: basket-api
      relation: CALLS
      target: catalog-api

expected_violated_contracts:
  - ES-C001