schema_version: "1.0"

mutation_id: AS-M02
system_id: astronomy-shop

description: >
  Introduce an unexpected direct CALLS relation from payment to shipping.

rationale: >
  Tests non-runtime detection of an unexpected service dependency.

operations:
  - operation: ADD_RELATION
    relation:
      source: payment
      relation: CALLS
      target: shipping

expected_delta:
  added_nodes: []
  removed_nodes: []

  added_relations:
    - source: payment
      relation: CALLS
      target: shipping

  removed_relations: []

expected_violated_contracts:
  - AS-C002