schema_version: "1.0"

mutation_id: TS-M01
system_id: teastore

description: >
  Introduce an unexpected direct CALLS relation from WebUI to Persistence.

rationale: >
  Runtime-visible controlled service dependency.

operations:
  - operation: ADD_RELATION
    relation:
      source: webui
      relation: CALLS
      target: persistence

expected_delta:
  added_relations:
    - source: webui
      relation: CALLS
      target: persistence

expected_violated_contracts:
  - TS-C001