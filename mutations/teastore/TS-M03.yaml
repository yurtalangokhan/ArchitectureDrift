schema_version: "1.0"

mutation_id: TS-M03
system_id: teastore

description: >
  Introduce an unexpected direct CALLS relation from Recommender
  to Authentication.

rationale: >
  Controlled dependency visible through both evidence channels.

operations:
  - operation: ADD_RELATION
    relation:
      source: recommender
      relation: CALLS
      target: auth

expected_delta:
  added_relations:
    - source: recommender
      relation: CALLS
      target: auth

expected_violated_contracts:
  - TS-C003