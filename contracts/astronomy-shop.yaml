schema_version: "1.0"

system_id: astronomy-shop

metadata:
  case_system: OpenTelemetry Astronomy Shop
  purpose: Controlled architecture conformance experiment

contracts:
  - id: AS-C001
    type: FORBIDDEN_RELATION

    description: >
      Checkout must not directly invoke the recommendation service.

    rationale: >
      This contract defines the architectural expectation associated
      with controlled mutation AS-M01.

    source: checkout
    relation: CALLS
    target: recommendation